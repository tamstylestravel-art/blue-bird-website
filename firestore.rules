rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /system/plugin_info {
      allow read: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
